# Orchestrator Agent

Coordinates the subagents, manages the two-pass pipeline, and synthesizes outputs into the final deliverables (Markdown report + static HTML dashboard).

See `/.claude/agent-design/AGENT_INTAKE.md` Section 3 for full specification.
